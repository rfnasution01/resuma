export default {
  plugins: {
    // The Tailwind CSS plugin for PostCSS to process Tailwind's utility-first classes
    tailwindcss: {},

    // The Autoprefixer plugin automatically adds vendor prefixes to your CSS to ensure cross-browser compatibility
    autoprefixer: {},
  },
}
